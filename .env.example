# Plantilla de variables de entorno.
# Copia este fichero a .env y rellena los valores reales:
#   cp .env.example .env En linux
# El fichero .env NO se sube al repositorio (esta en .gitignore), por protección de los datos; este .example si, usa este :D.

# Usuario y contraseña del almacen de tripletas Fuseki. Deja admin para logearte con sus credenciales en Fuseki, cambia en función del proyecto
FUSEKI_USER=admin
FUSEKI_PASSWORD=cambiame_soy_una_contraseña_de_prueba

# Clave secreta para firmar los tokens JWT (usa una cadena larga y aleatoria)
JWT_SECRET=cambiame_soy_una_contraseña_de_prueba

# Caducidad del token JWT (configurable). Acepta el formato de la librería
# jsonwebtoken: "24h", "7d", "30m", "3600" (segundos), etc. Por defecto 24h.
JWT_EXPIRES_IN=24h
